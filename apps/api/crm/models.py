"""
Domain models — Client, Action, Document.

Phase 2 fleshes these out. The intended pattern (ported from the legacy
desktop app) is: encrypted JSON `payload` column for sensitive fields,
denormalized plaintext columns for searchable/filterable fields.
"""
# Models intentionally empty until Phase 2.
